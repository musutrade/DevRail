/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { DevRailRunPage } from '../../models/dev-rail-run-page';

export interface ListDevRailRuns$Params {
  task_id: number;
  page?: number;
  pageSize?: number;
  keyword?: string;
  status?: string;
  projectId?: number;
}

export function listDevRailRuns(http: HttpClient, rootUrl: string, params: ListDevRailRuns$Params, context?: HttpContext): Observable<StrictHttpResponse<DevRailRunPage>> {
  const rb = new RequestBuilder(rootUrl, listDevRailRuns.PATH, 'get');
  if (params) {
    rb.path('task_id', params.task_id, {});
    rb.query('page', params.page, {});
    rb.query('pageSize', params.pageSize, {});
    rb.query('keyword', params.keyword, {});
    rb.query('status', params.status, {});
    rb.query('projectId', params.projectId, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<DevRailRunPage>;
    })
  );
}

listDevRailRuns.PATH = '/tasks/{task_id}/runs';
