/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { DevRailQualityGatePage } from '../../models/dev-rail-quality-gate-page';

export interface ExecuteDevRailRunQualityGates$Params {
  id: number;
}

export function executeDevRailRunQualityGates(http: HttpClient, rootUrl: string, params: ExecuteDevRailRunQualityGates$Params, context?: HttpContext): Observable<StrictHttpResponse<DevRailQualityGatePage>> {
  const rb = new RequestBuilder(rootUrl, executeDevRailRunQualityGates.PATH, 'post');
  if (params) {
    rb.path('id', params.id, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<DevRailQualityGatePage>;
    })
  );
}

executeDevRailRunQualityGates.PATH = '/runs/{id}/quality-gates/execute';
