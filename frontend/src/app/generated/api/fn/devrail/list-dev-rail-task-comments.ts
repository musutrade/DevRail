/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { DevRailTaskCommentPage } from '../../models/dev-rail-task-comment-page';

export interface ListDevRailTaskComments$Params {
  task_id: number;
  page?: number;
  pageSize?: number;
  keyword?: string;
  status?: string;
  projectId?: number;
  assigneeUserId?: number;
  label?: string;
}

export function listDevRailTaskComments(http: HttpClient, rootUrl: string, params: ListDevRailTaskComments$Params, context?: HttpContext): Observable<StrictHttpResponse<DevRailTaskCommentPage>> {
  const rb = new RequestBuilder(rootUrl, listDevRailTaskComments.PATH, 'get');
  if (params) {
    rb.path('task_id', params.task_id, {});
    rb.query('page', params.page, {});
    rb.query('pageSize', params.pageSize, {});
    rb.query('keyword', params.keyword, {});
    rb.query('status', params.status, {});
    rb.query('projectId', params.projectId, {});
    rb.query('assigneeUserId', params.assigneeUserId, {});
    rb.query('label', params.label, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<DevRailTaskCommentPage>;
    })
  );
}

listDevRailTaskComments.PATH = '/tasks/{task_id}/comments';
