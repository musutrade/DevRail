/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { DevRailProjectPage } from '../../models/dev-rail-project-page';

export interface ListDevRailProjects$Params {
  page?: number;
  pageSize?: number;
  keyword?: string;
  status?: string;
  projectId?: number;
  assigneeUserId?: number;
  label?: string;
}

export function listDevRailProjects(http: HttpClient, rootUrl: string, params?: ListDevRailProjects$Params, context?: HttpContext): Observable<StrictHttpResponse<DevRailProjectPage>> {
  const rb = new RequestBuilder(rootUrl, listDevRailProjects.PATH, 'get');
  if (params) {
    rb.query('page', params.page, {});
    rb.query('pageSize', params.pageSize, {});
    rb.query('keyword', params.keyword, {});
    rb.query('status', params.status, {});
    rb.query('projectId', params.projectId, {});
    rb.query('assigneeUserId', params.assigneeUserId, {});
    rb.query('label', params.label, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<DevRailProjectPage>;
    })
  );
}

listDevRailProjects.PATH = '/projects';
