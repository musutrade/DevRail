/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { CreateDevRailRunRequest } from '../../models/create-dev-rail-run-request';
import { DevRailRunResponse } from '../../models/dev-rail-run-response';

export interface CreateDevRailRun$Params {
  task_id: number;
      body: CreateDevRailRunRequest
}

export function createDevRailRun(http: HttpClient, rootUrl: string, params: CreateDevRailRun$Params, context?: HttpContext): Observable<StrictHttpResponse<DevRailRunResponse>> {
  const rb = new RequestBuilder(rootUrl, createDevRailRun.PATH, 'post');
  if (params) {
    rb.path('task_id', params.task_id, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<DevRailRunResponse>;
    })
  );
}

createDevRailRun.PATH = '/tasks/{task_id}/runs';
