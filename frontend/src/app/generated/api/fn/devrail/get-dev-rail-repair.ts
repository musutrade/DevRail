/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { DevRailRepairResponse } from '../../models/dev-rail-repair-response';

export interface GetDevRailRepair$Params {
  id: number;
}

export function getDevRailRepair(http: HttpClient, rootUrl: string, params: GetDevRailRepair$Params, context?: HttpContext): Observable<StrictHttpResponse<DevRailRepairResponse>> {
  const rb = new RequestBuilder(rootUrl, getDevRailRepair.PATH, 'get');
  if (params) {
    rb.path('id', params.id, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<DevRailRepairResponse>;
    })
  );
}

getDevRailRepair.PATH = '/repairs/{id}';
