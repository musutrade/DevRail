/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { DevRailRunResponse } from '../../models/dev-rail-run-response';

export interface GetDevRailRun$Params {
  id: number;
}

export function getDevRailRun(http: HttpClient, rootUrl: string, params: GetDevRailRun$Params, context?: HttpContext): Observable<StrictHttpResponse<DevRailRunResponse>> {
  const rb = new RequestBuilder(rootUrl, getDevRailRun.PATH, 'get');
  if (params) {
    rb.path('id', params.id, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<DevRailRunResponse>;
    })
  );
}

getDevRailRun.PATH = '/runs/{id}';
