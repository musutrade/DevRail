/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { DevRailQualityGateLogPage } from '../../models/dev-rail-quality-gate-log-page';

export interface GetDevRailRunQualityGateLog$Params {
  id: number;
  log_ref: string;
  after_cursor: (number | null);
  limit: (number | null);
}

export function getDevRailRunQualityGateLog(http: HttpClient, rootUrl: string, params: GetDevRailRunQualityGateLog$Params, context?: HttpContext): Observable<StrictHttpResponse<DevRailQualityGateLogPage>> {
  const rb = new RequestBuilder(rootUrl, getDevRailRunQualityGateLog.PATH, 'get');
  if (params) {
    rb.path('id', params.id, {});
    rb.path('log_ref', params.log_ref, {});
    rb.path('after_cursor', params.after_cursor, {});
    rb.path('limit', params.limit, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<DevRailQualityGateLogPage>;
    })
  );
}

getDevRailRunQualityGateLog.PATH = '/runs/{id}/quality-gate-log';
