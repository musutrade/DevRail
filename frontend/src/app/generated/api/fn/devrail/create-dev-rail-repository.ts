/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { CreateDevRailRepositoryRequest } from '../../models/create-dev-rail-repository-request';
import { DevRailRepositoryResponse } from '../../models/dev-rail-repository-response';

export interface CreateDevRailRepository$Params {
  project_id: number;
      body: CreateDevRailRepositoryRequest
}

export function createDevRailRepository(http: HttpClient, rootUrl: string, params: CreateDevRailRepository$Params, context?: HttpContext): Observable<StrictHttpResponse<DevRailRepositoryResponse>> {
  const rb = new RequestBuilder(rootUrl, createDevRailRepository.PATH, 'post');
  if (params) {
    rb.path('project_id', params.project_id, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<DevRailRepositoryResponse>;
    })
  );
}

createDevRailRepository.PATH = '/projects/{project_id}/repositories';
