/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { DevRailRepositoryResponse } from '../../models/dev-rail-repository-response';

export interface SyncDevRailRepository$Params {
  project_id: number;
  id: number;
}

export function syncDevRailRepository(http: HttpClient, rootUrl: string, params: SyncDevRailRepository$Params, context?: HttpContext): Observable<StrictHttpResponse<DevRailRepositoryResponse>> {
  const rb = new RequestBuilder(rootUrl, syncDevRailRepository.PATH, 'post');
  if (params) {
    rb.path('project_id', params.project_id, {});
    rb.path('id', params.id, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<DevRailRepositoryResponse>;
    })
  );
}

syncDevRailRepository.PATH = '/projects/{project_id}/repositories/{id}/sync';
