/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { DevRailEnvironmentPage } from '../../models/dev-rail-environment-page';

export interface ListDevRailEnvironments$Params {
  project_id: number;
  page?: number;
  pageSize?: number;
  keyword?: string;
  status?: string;
  projectId?: number;
}

export function listDevRailEnvironments(http: HttpClient, rootUrl: string, params: ListDevRailEnvironments$Params, context?: HttpContext): Observable<StrictHttpResponse<DevRailEnvironmentPage>> {
  const rb = new RequestBuilder(rootUrl, listDevRailEnvironments.PATH, 'get');
  if (params) {
    rb.path('project_id', params.project_id, {});
    rb.query('page', params.page, {});
    rb.query('pageSize', params.pageSize, {});
    rb.query('keyword', params.keyword, {});
    rb.query('status', params.status, {});
    rb.query('projectId', params.projectId, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<DevRailEnvironmentPage>;
    })
  );
}

listDevRailEnvironments.PATH = '/projects/{project_id}/environments';
