/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { DevRailEnvironmentResponse } from '../../models/dev-rail-environment-response';
import { UpdateDevRailEnvironmentRequest } from '../../models/update-dev-rail-environment-request';

export interface UpdateDevRailEnvironment$Params {
  project_id: number;
  id: number;
      body: UpdateDevRailEnvironmentRequest
}

export function updateDevRailEnvironment(http: HttpClient, rootUrl: string, params: UpdateDevRailEnvironment$Params, context?: HttpContext): Observable<StrictHttpResponse<DevRailEnvironmentResponse>> {
  const rb = new RequestBuilder(rootUrl, updateDevRailEnvironment.PATH, 'patch');
  if (params) {
    rb.path('project_id', params.project_id, {});
    rb.path('id', params.id, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<DevRailEnvironmentResponse>;
    })
  );
}

updateDevRailEnvironment.PATH = '/projects/{project_id}/environments/{id}';
