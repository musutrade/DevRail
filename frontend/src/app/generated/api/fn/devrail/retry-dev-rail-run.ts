/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { DevRailRunResponse } from '../../models/dev-rail-run-response';
import { RetryDevRailRunRequest } from '../../models/retry-dev-rail-run-request';

export interface RetryDevRailRun$Params {
  id: number;
      body: RetryDevRailRunRequest
}

export function retryDevRailRun(http: HttpClient, rootUrl: string, params: RetryDevRailRun$Params, context?: HttpContext): Observable<StrictHttpResponse<DevRailRunResponse>> {
  const rb = new RequestBuilder(rootUrl, retryDevRailRun.PATH, 'post');
  if (params) {
    rb.path('id', params.id, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<DevRailRunResponse>;
    })
  );
}

retryDevRailRun.PATH = '/runs/{id}/retry';
