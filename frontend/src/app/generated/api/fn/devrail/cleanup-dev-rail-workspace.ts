/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { DevRailTaskWorkspaceResponse } from '../../models/dev-rail-task-workspace-response';

export interface CleanupDevRailWorkspace$Params {
  id: number;
}

export function cleanupDevRailWorkspace(http: HttpClient, rootUrl: string, params: CleanupDevRailWorkspace$Params, context?: HttpContext): Observable<StrictHttpResponse<DevRailTaskWorkspaceResponse>> {
  const rb = new RequestBuilder(rootUrl, cleanupDevRailWorkspace.PATH, 'post');
  if (params) {
    rb.path('id', params.id, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<DevRailTaskWorkspaceResponse>;
    })
  );
}

cleanupDevRailWorkspace.PATH = '/workspaces/{id}/cleanup';
