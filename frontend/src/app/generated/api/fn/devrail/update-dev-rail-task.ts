/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { DevRailTaskResponse } from '../../models/dev-rail-task-response';
import { UpdateDevRailTaskRequest } from '../../models/update-dev-rail-task-request';

export interface UpdateDevRailTask$Params {
  project_id: number;
  id: number;
      body: UpdateDevRailTaskRequest
}

export function updateDevRailTask(http: HttpClient, rootUrl: string, params: UpdateDevRailTask$Params, context?: HttpContext): Observable<StrictHttpResponse<DevRailTaskResponse>> {
  const rb = new RequestBuilder(rootUrl, updateDevRailTask.PATH, 'patch');
  if (params) {
    rb.path('project_id', params.project_id, {});
    rb.path('id', params.id, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<DevRailTaskResponse>;
    })
  );
}

updateDevRailTask.PATH = '/projects/{project_id}/tasks/{id}';
