/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { DevRailRepositoryPage } from '../../models/dev-rail-repository-page';

export interface ListDevRailRepositories$Params {
  project_id: number;
  page?: number;
  pageSize?: number;
  keyword?: string;
  status?: string;
  projectId?: number;
  assigneeUserId?: number;
  label?: string;
  taskId?: number;
  runId?: number;
}

export function listDevRailRepositories(http: HttpClient, rootUrl: string, params: ListDevRailRepositories$Params, context?: HttpContext): Observable<StrictHttpResponse<DevRailRepositoryPage>> {
  const rb = new RequestBuilder(rootUrl, listDevRailRepositories.PATH, 'get');
  if (params) {
    rb.path('project_id', params.project_id, {});
    rb.query('page', params.page, {});
    rb.query('pageSize', params.pageSize, {});
    rb.query('keyword', params.keyword, {});
    rb.query('status', params.status, {});
    rb.query('projectId', params.projectId, {});
    rb.query('assigneeUserId', params.assigneeUserId, {});
    rb.query('label', params.label, {});
    rb.query('taskId', params.taskId, {});
    rb.query('runId', params.runId, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<DevRailRepositoryPage>;
    })
  );
}

listDevRailRepositories.PATH = '/projects/{project_id}/repositories';
