/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { DevRailApprovalPage } from '../../models/dev-rail-approval-page';

export interface ListDevRailApprovals$Params {
  page?: number;
  pageSize?: number;
  keyword?: string;
  status?: string;
  projectId?: number;
}

export function listDevRailApprovals(http: HttpClient, rootUrl: string, params?: ListDevRailApprovals$Params, context?: HttpContext): Observable<StrictHttpResponse<DevRailApprovalPage>> {
  const rb = new RequestBuilder(rootUrl, listDevRailApprovals.PATH, 'get');
  if (params) {
    rb.query('page', params.page, {});
    rb.query('pageSize', params.pageSize, {});
    rb.query('keyword', params.keyword, {});
    rb.query('status', params.status, {});
    rb.query('projectId', params.projectId, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<DevRailApprovalPage>;
    })
  );
}

listDevRailApprovals.PATH = '/approvals';
