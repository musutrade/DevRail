/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { CreateDevRailEnvironmentRequest } from '../../models/create-dev-rail-environment-request';
import { DevRailEnvironmentResponse } from '../../models/dev-rail-environment-response';

export interface CreateDevRailEnvironment$Params {
  project_id: number;
      body: CreateDevRailEnvironmentRequest
}

export function createDevRailEnvironment(http: HttpClient, rootUrl: string, params: CreateDevRailEnvironment$Params, context?: HttpContext): Observable<StrictHttpResponse<DevRailEnvironmentResponse>> {
  const rb = new RequestBuilder(rootUrl, createDevRailEnvironment.PATH, 'post');
  if (params) {
    rb.path('project_id', params.project_id, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<DevRailEnvironmentResponse>;
    })
  );
}

createDevRailEnvironment.PATH = '/projects/{project_id}/environments';
