/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { DevRailEnvironmentHealthResponse } from '../../models/dev-rail-environment-health-response';

export interface HealthCheckDevRailEnvironment$Params {
  project_id: number;
  id: number;
}

export function healthCheckDevRailEnvironment(http: HttpClient, rootUrl: string, params: HealthCheckDevRailEnvironment$Params, context?: HttpContext): Observable<StrictHttpResponse<DevRailEnvironmentHealthResponse>> {
  const rb = new RequestBuilder(rootUrl, healthCheckDevRailEnvironment.PATH, 'post');
  if (params) {
    rb.path('project_id', params.project_id, {});
    rb.path('id', params.id, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<DevRailEnvironmentHealthResponse>;
    })
  );
}

healthCheckDevRailEnvironment.PATH = '/projects/{project_id}/environments/{id}/health-check';
