/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { DevRailTaskEventPage } from '../../models/dev-rail-task-event-page';

export interface ListDevRailTaskEvents$Params {
  task_id: number;
  after_cursor?: number;
  limit?: number;
}

export function listDevRailTaskEvents(http: HttpClient, rootUrl: string, params: ListDevRailTaskEvents$Params, context?: HttpContext): Observable<StrictHttpResponse<DevRailTaskEventPage>> {
  const rb = new RequestBuilder(rootUrl, listDevRailTaskEvents.PATH, 'get');
  if (params) {
    rb.path('task_id', params.task_id, {});
    rb.query('after_cursor', params.after_cursor, {});
    rb.query('limit', params.limit, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<DevRailTaskEventPage>;
    })
  );
}

listDevRailTaskEvents.PATH = '/tasks/{task_id}/events';
