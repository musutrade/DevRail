/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { DevRailRepairResponse } from '../../models/dev-rail-repair-response';

export interface CancelDevRailRepair$Params {
  id: number;
}

export function cancelDevRailRepair(http: HttpClient, rootUrl: string, params: CancelDevRailRepair$Params, context?: HttpContext): Observable<StrictHttpResponse<DevRailRepairResponse>> {
  const rb = new RequestBuilder(rootUrl, cancelDevRailRepair.PATH, 'post');
  if (params) {
    rb.path('id', params.id, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<DevRailRepairResponse>;
    })
  );
}

cancelDevRailRepair.PATH = '/repairs/{id}/cancel';
