/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { DevRailRepositoryResponse } from '../../models/dev-rail-repository-response';
import { UpdateDevRailRepositoryRequest } from '../../models/update-dev-rail-repository-request';

export interface UpdateDevRailRepository$Params {
  project_id: number;
  id: number;
      body: UpdateDevRailRepositoryRequest
}

export function updateDevRailRepository(http: HttpClient, rootUrl: string, params: UpdateDevRailRepository$Params, context?: HttpContext): Observable<StrictHttpResponse<DevRailRepositoryResponse>> {
  const rb = new RequestBuilder(rootUrl, updateDevRailRepository.PATH, 'patch');
  if (params) {
    rb.path('project_id', params.project_id, {});
    rb.path('id', params.id, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<DevRailRepositoryResponse>;
    })
  );
}

updateDevRailRepository.PATH = '/projects/{project_id}/repositories/{id}';
