/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { DecideDevRailReviewRequest } from '../../models/decide-dev-rail-review-request';
import { DevRailReviewResponse } from '../../models/dev-rail-review-response';

export interface DecideDevRailReview$Params {
  id: number;
      body: DecideDevRailReviewRequest
}

export function decideDevRailReview(http: HttpClient, rootUrl: string, params: DecideDevRailReview$Params, context?: HttpContext): Observable<StrictHttpResponse<DevRailReviewResponse>> {
  const rb = new RequestBuilder(rootUrl, decideDevRailReview.PATH, 'post');
  if (params) {
    rb.path('id', params.id, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<DevRailReviewResponse>;
    })
  );
}

decideDevRailReview.PATH = '/reviews/{id}/decide';
