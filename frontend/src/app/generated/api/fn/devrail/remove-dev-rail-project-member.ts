/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';


export interface RemoveDevRailProjectMember$Params {
  project_id: number;
  user_id: number;
}

export function removeDevRailProjectMember(http: HttpClient, rootUrl: string, params: RemoveDevRailProjectMember$Params, context?: HttpContext): Observable<StrictHttpResponse<void>> {
  const rb = new RequestBuilder(rootUrl, removeDevRailProjectMember.PATH, 'delete');
  if (params) {
    rb.path('project_id', params.project_id, {});
    rb.path('user_id', params.user_id, {});
  }

  return http.request(
    rb.build({ responseType: 'text', accept: '*/*', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return (r as HttpResponse<any>).clone({ body: undefined }) as StrictHttpResponse<void>;
    })
  );
}

removeDevRailProjectMember.PATH = '/projects/{project_id}/members/{user_id}';
