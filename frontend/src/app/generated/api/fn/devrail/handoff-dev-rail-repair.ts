/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { DevRailRepairHandoffRequest } from '../../models/dev-rail-repair-handoff-request';
import { DevRailRepairResponse } from '../../models/dev-rail-repair-response';

export interface HandoffDevRailRepair$Params {
  id: number;
      body: DevRailRepairHandoffRequest
}

export function handoffDevRailRepair(http: HttpClient, rootUrl: string, params: HandoffDevRailRepair$Params, context?: HttpContext): Observable<StrictHttpResponse<DevRailRepairResponse>> {
  const rb = new RequestBuilder(rootUrl, handoffDevRailRepair.PATH, 'post');
  if (params) {
    rb.path('id', params.id, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<DevRailRepairResponse>;
    })
  );
}

handoffDevRailRepair.PATH = '/repairs/{id}/handoff';
