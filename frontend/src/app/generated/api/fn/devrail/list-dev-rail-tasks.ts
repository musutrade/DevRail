/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { DevRailTaskPage } from '../../models/dev-rail-task-page';

export interface ListDevRailTasks$Params {
  project_id: number;
  page?: number;
  pageSize?: number;
  keyword?: string;
  status?: string;
  projectId?: number;
}

export function listDevRailTasks(http: HttpClient, rootUrl: string, params: ListDevRailTasks$Params, context?: HttpContext): Observable<StrictHttpResponse<DevRailTaskPage>> {
  const rb = new RequestBuilder(rootUrl, listDevRailTasks.PATH, 'get');
  if (params) {
    rb.path('project_id', params.project_id, {});
    rb.query('page', params.page, {});
    rb.query('pageSize', params.pageSize, {});
    rb.query('keyword', params.keyword, {});
    rb.query('status', params.status, {});
    rb.query('projectId', params.projectId, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<DevRailTaskPage>;
    })
  );
}

listDevRailTasks.PATH = '/projects/{project_id}/tasks';
