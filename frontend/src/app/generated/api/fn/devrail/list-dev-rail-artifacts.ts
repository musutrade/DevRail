/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { DevRailArtifactPage } from '../../models/dev-rail-artifact-page';

export interface ListDevRailArtifacts$Params {
  page?: number;
  page_size?: number;
  task_id?: number;
  run_id?: number;
  artifact_type?: string;
}

export function listDevRailArtifacts(http: HttpClient, rootUrl: string, params?: ListDevRailArtifacts$Params, context?: HttpContext): Observable<StrictHttpResponse<DevRailArtifactPage>> {
  const rb = new RequestBuilder(rootUrl, listDevRailArtifacts.PATH, 'get');
  if (params) {
    rb.query('page', params.page, {});
    rb.query('page_size', params.page_size, {});
    rb.query('task_id', params.task_id, {});
    rb.query('run_id', params.run_id, {});
    rb.query('artifact_type', params.artifact_type, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<DevRailArtifactPage>;
    })
  );
}

listDevRailArtifacts.PATH = '/artifacts';
