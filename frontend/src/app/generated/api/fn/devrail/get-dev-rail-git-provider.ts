/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { DevRailGitProviderResponse } from '../../models/dev-rail-git-provider-response';

export interface GetDevRailGitProvider$Params {
  project_id: number;
  id: number;
}

export function getDevRailGitProvider(http: HttpClient, rootUrl: string, params: GetDevRailGitProvider$Params, context?: HttpContext): Observable<StrictHttpResponse<DevRailGitProviderResponse>> {
  const rb = new RequestBuilder(rootUrl, getDevRailGitProvider.PATH, 'get');
  if (params) {
    rb.path('project_id', params.project_id, {});
    rb.path('id', params.id, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<DevRailGitProviderResponse>;
    })
  );
}

getDevRailGitProvider.PATH = '/projects/{project_id}/repositories/{id}/git-provider';
