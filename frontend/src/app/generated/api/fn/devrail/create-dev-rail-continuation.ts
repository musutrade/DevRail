/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { CreateDevRailContinuationRequest } from '../../models/create-dev-rail-continuation-request';
import { DevRailContinuationResponse } from '../../models/dev-rail-continuation-response';

export interface CreateDevRailContinuation$Params {
  id: number;
      body: CreateDevRailContinuationRequest
}

export function createDevRailContinuation(http: HttpClient, rootUrl: string, params: CreateDevRailContinuation$Params, context?: HttpContext): Observable<StrictHttpResponse<DevRailContinuationResponse>> {
  const rb = new RequestBuilder(rootUrl, createDevRailContinuation.PATH, 'post');
  if (params) {
    rb.path('id', params.id, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<DevRailContinuationResponse>;
    })
  );
}

createDevRailContinuation.PATH = '/runs/{id}/continuations';
