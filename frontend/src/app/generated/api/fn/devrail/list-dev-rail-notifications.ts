/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { DevRailNotificationPage } from '../../models/dev-rail-notification-page';

export interface ListDevRailNotifications$Params {
  page?: number;
  pageSize?: number;
  keyword?: string;
  status?: string;
  projectId?: number;
  assigneeUserId?: number;
  label?: string;
  taskId?: number;
  runId?: number;
}

export function listDevRailNotifications(http: HttpClient, rootUrl: string, params?: ListDevRailNotifications$Params, context?: HttpContext): Observable<StrictHttpResponse<DevRailNotificationPage>> {
  const rb = new RequestBuilder(rootUrl, listDevRailNotifications.PATH, 'get');
  if (params) {
    rb.query('page', params.page, {});
    rb.query('pageSize', params.pageSize, {});
    rb.query('keyword', params.keyword, {});
    rb.query('status', params.status, {});
    rb.query('projectId', params.projectId, {});
    rb.query('assigneeUserId', params.assigneeUserId, {});
    rb.query('label', params.label, {});
    rb.query('taskId', params.taskId, {});
    rb.query('runId', params.runId, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<DevRailNotificationPage>;
    })
  );
}

listDevRailNotifications.PATH = '/notifications';
