/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { DevRailRunEventPage } from '../../models/dev-rail-run-event-page';

export interface ListDevRailRunEvents$Params {
  id: number;
}

export function listDevRailRunEvents(http: HttpClient, rootUrl: string, params: ListDevRailRunEvents$Params, context?: HttpContext): Observable<StrictHttpResponse<DevRailRunEventPage>> {
  const rb = new RequestBuilder(rootUrl, listDevRailRunEvents.PATH, 'get');
  if (params) {
    rb.path('id', params.id, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<DevRailRunEventPage>;
    })
  );
}

listDevRailRunEvents.PATH = '/runs/{id}/events';
