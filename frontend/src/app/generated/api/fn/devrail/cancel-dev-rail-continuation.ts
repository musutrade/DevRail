/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { DevRailContinuationResponse } from '../../models/dev-rail-continuation-response';

export interface CancelDevRailContinuation$Params {
  id: number;
}

export function cancelDevRailContinuation(http: HttpClient, rootUrl: string, params: CancelDevRailContinuation$Params, context?: HttpContext): Observable<StrictHttpResponse<DevRailContinuationResponse>> {
  const rb = new RequestBuilder(rootUrl, cancelDevRailContinuation.PATH, 'post');
  if (params) {
    rb.path('id', params.id, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<DevRailContinuationResponse>;
    })
  );
}

cancelDevRailContinuation.PATH = '/continuations/{id}/cancel';
