/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { DevRailTaskWorkspaceResponse } from '../../models/dev-rail-task-workspace-response';

export interface GetDevRailRunWorkspace$Params {
  id: number;
}

export function getDevRailRunWorkspace(http: HttpClient, rootUrl: string, params: GetDevRailRunWorkspace$Params, context?: HttpContext): Observable<StrictHttpResponse<DevRailTaskWorkspaceResponse>> {
  const rb = new RequestBuilder(rootUrl, getDevRailRunWorkspace.PATH, 'get');
  if (params) {
    rb.path('id', params.id, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<DevRailTaskWorkspaceResponse>;
    })
  );
}

getDevRailRunWorkspace.PATH = '/runs/{id}/workspace';
