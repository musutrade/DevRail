/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { CreateDevRailTaskRequest } from '../../models/create-dev-rail-task-request';
import { DevRailTaskResponse } from '../../models/dev-rail-task-response';

export interface CreateDevRailTask$Params {
  project_id: number;
      body: CreateDevRailTaskRequest
}

export function createDevRailTask(http: HttpClient, rootUrl: string, params: CreateDevRailTask$Params, context?: HttpContext): Observable<StrictHttpResponse<DevRailTaskResponse>> {
  const rb = new RequestBuilder(rootUrl, createDevRailTask.PATH, 'post');
  if (params) {
    rb.path('project_id', params.project_id, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<DevRailTaskResponse>;
    })
  );
}

createDevRailTask.PATH = '/projects/{project_id}/tasks';
