/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { DevRailChangesetResponse } from '../../models/dev-rail-changeset-response';

export interface GetDevRailRunChangeset$Params {
  id: number;
}

export function getDevRailRunChangeset(http: HttpClient, rootUrl: string, params: GetDevRailRunChangeset$Params, context?: HttpContext): Observable<StrictHttpResponse<DevRailChangesetResponse>> {
  const rb = new RequestBuilder(rootUrl, getDevRailRunChangeset.PATH, 'get');
  if (params) {
    rb.path('id', params.id, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<DevRailChangesetResponse>;
    })
  );
}

getDevRailRunChangeset.PATH = '/runs/{id}/changeset';
