/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { DevRailTaskCommentResponse } from '../../models/dev-rail-task-comment-response';
import { UpdateDevRailTaskCommentRequest } from '../../models/update-dev-rail-task-comment-request';

export interface UpdateDevRailTaskComment$Params {
  id: number;
      body: UpdateDevRailTaskCommentRequest
}

export function updateDevRailTaskComment(http: HttpClient, rootUrl: string, params: UpdateDevRailTaskComment$Params, context?: HttpContext): Observable<StrictHttpResponse<DevRailTaskCommentResponse>> {
  const rb = new RequestBuilder(rootUrl, updateDevRailTaskComment.PATH, 'patch');
  if (params) {
    rb.path('id', params.id, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<DevRailTaskCommentResponse>;
    })
  );
}

updateDevRailTaskComment.PATH = '/task-comments/{id}';
