/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { DevRailTaskWorkspaceResponse } from '../../models/dev-rail-task-workspace-response';

export interface GetDevRailTaskWorkspace$Params {
  task_id: number;
}

export function getDevRailTaskWorkspace(http: HttpClient, rootUrl: string, params: GetDevRailTaskWorkspace$Params, context?: HttpContext): Observable<StrictHttpResponse<(null | DevRailTaskWorkspaceResponse)>> {
  const rb = new RequestBuilder(rootUrl, getDevRailTaskWorkspace.PATH, 'get');
  if (params) {
    rb.path('task_id', params.task_id, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<(null | DevRailTaskWorkspaceResponse)>;
    })
  );
}

getDevRailTaskWorkspace.PATH = '/tasks/{task_id}/workspace';
